apiVersion: v1
kind: Service
metadata:
  name: python-helloworld-service

spec:
  type: NodePort

  selector:
    app: python-helloworld-app

  ports:
    - protocol: TCP
      port: 80
      targetPort: 8000
      nodePort: 30080